= simple_form_for(@status_surat_masuk, html: { class: 'form-horizontal add-left-margin' }, defaults: { wrapper_html: { class: 'col-md-4 mb-2' } } ) do |f|
  - if @status_surat_masuk.errors.any?
    #error_explanation
      %h2= "#{pluralize(@status_surat_masuk.errors.count, "error")} menyebabkan Status Surat Masuk yang dibuat tidak dapat disimpan:"
      %ul
        - @status_surat_masuk.errors.full_messages.each do |message|
          %li= message
  .row
    = f.input :nama, label_method: :nama
  .row
    = button_tag(type: 'submit', class: "btn btn-primary") do
      %i.fa.d-inline.fa-lg.fa-save
      Simpan